import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Settings = () => {
  return (
    <View style={{ flex: 1, backgroundColor: "white", padding: 20 }}></View>
  );
};

export default Settings;

const styles = StyleSheet.create({});
